version: '3.8'

services:

  backend:
    build: ./backend
    volumes:
      - type: bind
        source: ./backend/app
        target: /code/app
    ports:
      - "8000:8000"
    command: uvicorn app.main:app --root-path /api --proxy-headers --host 0.0.0.0 --reload

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      - backend

volumes:
  caddy-data:
  caddy-config:
